---
# Only the main Sass file needs front matter (the dashes are enough)
search: false
---

@charset "utf-8";

@import "imca-cat/skins/{{ site.imca_cat_skin | default: 'default' }}"; // skin
@import "imca-cat"; // main partials